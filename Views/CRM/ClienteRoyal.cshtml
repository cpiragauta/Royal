@model CinemaPOS.ModelosPropios.Util.ClientesRoyal
@{
    ViewBag.Title = "ClienteRoyal";
    Layout = "~/Views/Inicio/Diseño.cshtml";
}

<!--jQuery [ REQUIRED ]-->
<script src="~/Content/js/jquery-2.2.4.min.js"></script>
<!--DataTables [ OPTIONAL ]-->
<script src="~/Content/plugins/datatables/media/js/jquery.dataTables.js"></script>
<script src="~/Content/plugins/datatables/media/js/dataTables.bootstrap.js"></script>
<script src="~/Content/plugins/datatables/extensions/Responsive/js/dataTables.responsive.min.js"></script>
<!--DataTables Sample [ SAMPLE ]-->
<script src="~/Content/js/demo/tables-datatables.js"></script>
@*<script src="~/Content/js/JqueryDataRables/jquery.dataTables.min.js"></script>
<link href="~/Content/Tables.min.css" rel="stylesheet" />
<link href="~/Content/css/JqueryDataTables/jquery.dataTables.min.css" rel="stylesheet" />*@
<p>
    <a class="btn btn-success" href='@Url.Action("NuevoClienteRoyal", "CRM")'>Crear Cliente Royal</a>
</p>

<div class="panel">
    <div class="panel-heading">
        <h3 class="panel-title">Lista Terceros</h3>
    </div>
    <div class="panel-body">
        <table id="demo-dt-basic" class="table table-striped table-bordered table-hover" cellspacing="0" style="width:100%">
            <thead>
                <tr>
                    <th>Nombre Completo</th>
                    <th>Documento</th>
                    <th>Correo</th>
                    <th>Tel&eacute;fono</th>
                    <th>Ciudad</th>
                    <th>Tarjeta ID</th>
                    <th>Clasificaci&oacute;n</th>
                    <th>Preferencias</th>
                    <th></th>
                </tr>
            </thead>
            @*<tfoot>
                <tr>
                    <th>ID</th>
                    <th>Nombre Completo</th>
                    <th>Documento</th>
                    <th>Correo</th>
                    <th>Telefono</th>
                    <th>Ciudad</th>
                    <th>Tarjeta ID</th>
                    <th>Clasificacion</th>
                    <th>Preferencias</th>
                    <th></th>
                </tr>
            </tfoot>*@
            <tbody>
                @foreach (var item in (List<CinemaPOS.ModelosPropios.Util.ClientesRoyal>)ViewBag.Listado)
                {
                    <tr>
                        <td>@item.nombreCompleto</td>
                        <td>@item.documento</td>
                        <td>@item.correo</td>
                        <td>@item.telefono</td>
                        <td>@item.ciudades</td>
                        <td>@item.tarjetaID</td>
                        <td>@item.clasificacion</td>
                        <td>@item.pref</td>
                        <td><a href="@Url.Action("NuevoClienteRoyal", "CRM", new { @rowid = item.rowid })"><i class="glyphicon glyphicon-pencil"></i></a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
@*<script>
    $(document).ready(function () { 
    var cont = 0;
    $('#demo-dt-basic tfoot th').each(function () {
        var title = $(this).text();
        var width = 'width: 100px;';
        var id = '';

        if (title == 'ID')
            id = 'id="ID"';

        if (title == 'Nombre Completo')
            id = ' id="Nombre_Completo"';

        if (title == 'Documento')
            id = 'id="Documento"';

        if (title == 'Telefono')
            id = 'id="Telefono"';

        if (title == 'Ciudad')
            id = 'id="Ciudad"';

        if (title == 'Tarjeta ID')
            id = 'id="Tarjeta_ID"';

        if (title == 'Clasificacion')
            id = 'id="Clasificacion"';

        if (title == 'Preferencias')
            id = 'id="Preferencias"';
        if (title != '')
            $(this).html('<input type="text" style="' + width + '" ' + id + ' placeholder="Buscar ' + title + '" />');
        else
            title = '';
        cont += 1;
    });
    var aoColumnDefsType = [
       //{ "sType": "title-numeric", "aTargets": [3] },
       //{ "sType": "title-numeric", "aTargets": [7] },
       { "bSortable": false, "aTargets": [0] }//,
       //{ "bSortable": false, "aTargets": [8] },
       //{ "bSortable": false, "aTargets": [9] },
       //{ "sClass": "center", "aTargets": [2, 3, 4, 5, 6, 7] }
    ];

    var table = $('#demo-dt-basic').DataTable({
        "processing": true,
        "bFilter": true,
        "bPaginate": true,
        "info": true,
        "bDestroy": true,        
        aLengthMenu: [
            [6],
            [6]
        ],
        "iDisplayLength": 6, 
        "order": [[2, "desc"]],
        "aoColumnDefs":aoColumnDefsType
        //"aaSorting": [[2,'desc']]   
    });

        // Apply the search
    table.columns().every(function () {
        var that = this;

        $('input', this.footer()).on('keyup change', function () {
            if (that.search() !== this.value) {
                that
                    .search(this.value)
                    .draw();
            }
        });
    });
    });
</script>*@